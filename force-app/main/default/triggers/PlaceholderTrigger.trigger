// Placeholder trigger so the CI pipeline's triggers/ directory exists out of
// the box. Delete this file (and the meta.xml alongside it) once your project
// has real triggers.
trigger PlaceholderTrigger on Account(before insert) {
}
